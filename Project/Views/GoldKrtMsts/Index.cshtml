@model IEnumerable<Project.Models.GoldKrtMst>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GoldCrt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GoldCrt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GoldTypeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GoldTypeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GoldTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
