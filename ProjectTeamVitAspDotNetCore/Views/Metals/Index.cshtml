@model IEnumerable<ProjectTeamVitAspDotNetCore.Models.Metal>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetalCrt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetalCrt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MetalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MetalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MetalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
